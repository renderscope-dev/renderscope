"""Renderer JSON data files bundled with the package.

In development mode, these are loaded from the monorepo's data/renderers/
directory.  In production mode (installed from wheel), these are bundled
inside the package.
"""

from __future__ import annotations
